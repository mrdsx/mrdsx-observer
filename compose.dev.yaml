services:
  backend:
    build: backend
    environment:
      DB_HOST: postgres
    ports:
      - "8000:8000"

  postgres:
    image: postgres:18.4-alpine
    environment:
      POSTGRES_USER: default
      POSTGRES_PASSWORD: password
      POSTGRES_DB: database
    ports:
      - "5432:5432"
